[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    unit: fast unit tests, no external dependencies
    integration: integration tests using real SQLite
    live: live API tests requiring credentials in .env (Gemini, Google Calendar)
